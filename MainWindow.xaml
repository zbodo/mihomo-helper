<?xml version="1.0" encoding="utf-8" ?>
<Window
    x:Class="MihomoTray.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="Mihomo">
    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TitleBar x:Name="AppTitleBar" Title="Mihomo">
            <TitleBar.IconSource>
                <ImageIconSource ImageSource="Assets/mihomo-wrench.png" />
            </TitleBar.IconSource>
        </TitleBar>

        <ScrollViewer
            Grid.Row="1"
            HorizontalAlignment="Stretch"
            VerticalScrollBarVisibility="Auto">
            <StackPanel
                x:Name="ContentPanel"
                MinWidth="360"
                MaxWidth="520"
                Padding="20,4,20,20"
                Spacing="16">
                <Border
                    Padding="16"
                    Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1"
                    CornerRadius="8">
                    <Grid ColumnSpacing="16" RowSpacing="8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <TextBlock Foreground="{ThemeResource TextFillColorSecondaryBrush}" Text="系统代理" />
                        <TextBlock x:Name="ProxyStatusText" Grid.Column="1" FontWeight="SemiBold" />
                        <TextBlock
                            Grid.Row="1"
                            Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                            Text="TUN" />
                        <TextBlock x:Name="TunStatusText" Grid.Row="1" Grid.Column="1" FontWeight="SemiBold" />
                        <TextBlock
                            Grid.Row="2"
                            Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                            Text="内核" />
                        <TextBlock
                            x:Name="KernelStatusText"
                            Grid.Row="2"
                            Grid.Column="1"
                            TextTrimming="CharacterEllipsis"
                            TextWrapping="Wrap" />
                    </Grid>
                </Border>

                <ContentControl
                    x:Name="KernelPathPanel"
                    HorizontalAlignment="Stretch"
                    HorizontalContentAlignment="Stretch"
                    IsEnabled="False">
                    <StackPanel Spacing="8">
                        <TextBlock Style="{StaticResource CaptionTextBlockStyle}" Text="内核路径" />
                        <Grid ColumnSpacing="8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <TextBox
                                x:Name="KernelPathBox"
                                PlaceholderText="选择或粘贴 mihomo 内核 exe" />
                            <Button
                                Grid.Column="1"
                                Click="OnBrowseKernel"
                                Content="浏览" />
                            <Button
                                Grid.Column="2"
                                Click="OnOpenKernelPath"
                                Content="打开" />
                        </Grid>
                    </StackPanel>
                </ContentControl>

                <StackPanel Spacing="8">
                    <TextBlock Style="{StaticResource CaptionTextBlockStyle}" Text="工作模式" />
                    <Grid ColumnSpacing="8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Button
                            HorizontalAlignment="Stretch"
                            Click="OnProxy"
                            Style="{StaticResource AccentButtonStyle}"
                            Content="系统代理" />
                        <Button
                            Grid.Column="1"
                            HorizontalAlignment="Stretch"
                            Click="OnTun"
                            Content="TUN" />
                    </Grid>
                    <Grid ColumnSpacing="8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Button HorizontalAlignment="Stretch" Click="OnOff" Content="关代理" />
                        <Button Grid.Column="1" HorizontalAlignment="Stretch" Click="OnToggle" Content="切换" />
                        <Button Grid.Column="2" HorizontalAlignment="Stretch" Click="OnStop" Content="停内核" />
                    </Grid>
                </StackPanel>

                <StackPanel Spacing="8">
                    <TextBlock Style="{StaticResource CaptionTextBlockStyle}" Text="计划任务" />
                    <Grid ColumnSpacing="8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Button HorizontalAlignment="Stretch" Click="OnInstallTask" Content="添加任务" />
                        <Button Grid.Column="1" HorizontalAlignment="Stretch" Click="OnRemoveTask" Content="删除任务" />
                    </Grid>
                </StackPanel>

                <HyperlinkButton
                    HorizontalAlignment="Left"
                    Click="OnRefresh"
                    Content="刷新状态" />
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Window>
